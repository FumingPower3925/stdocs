#
# This file follows the GitHub CODEOWNERS spec. Each line is a path
# pattern and the GitHub users or teams that own it. Pull requests
# that touch a path require review from at least one of the listed
# owners before merge.
#
# See: https://docs.github.com/en/repositories/creating-and-managing-repositories/about-code-owners

# Default: the project owner reviews everything. ("*" is GitHub's
# documented repo-wide pattern; a bare "/" matches nothing.)
*                        @FumingPower3925

# Translations: when a language gains a dedicated translator, add
# them as a co-owner here (e.g. "/README.es.md @FumingPower3925
# @translator") so translation PRs can be merged on the translator's
# approval. Until then the default owner covers these files too.
