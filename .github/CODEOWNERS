# CODEOWNERS
#
# This file follows the GitHub CODEOWNERS spec. Each line is a path
# pattern and the GitHub users or teams that own it. Pull requests
# that touch a path require review from at least one of the listed
# owners before merge.
#
# See: https://docs.github.com/en/repositories/creating-and-managing-repositories/about-code-owners

# Default: the project owner reviews everything.
/                         @FumingPower3925

# Translations: each language's translator is a co-owner, so a
# translation PR can be merged when the translator approves (no
# project-owner review strictly required for prose changes). The
# project owner remains the fallback so a PR can still be merged
# when the translator is unavailable.
/README.es.md            @FumingPower3925
/README.ca.md            @FumingPower3925
